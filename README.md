# La Hora — Site (Express + EJS + Tailwind)

## Prérequis

- Node.js 18+
- MongoDB (local ou Atlas)

## Configuration

1. Copie `.env.example` en `.env` et remplis tes valeurs (Mongo, mail…).
2. Installe les deps :

   ```bash
   npm install
