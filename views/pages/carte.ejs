<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="icon" href="/public/favicon-la-hora.ico" type="image/x-icon">
  <title>Carte <%= moment === 'matin' ? 'du Matin' : 'du Soir' %> – La Hora</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/public/assets/css/style.css" />
</head>

<body class="bg-black text-white font-sans">
  <%- include('../partials/header') %>

  <main class="pt-24 pb-16 px-4 sm:px-8 md:px-20">
    <div class="max-w-5xl mx-auto">
      <h1 class="text-4xl font-bold text-red-500 mb-10 text-center">
        <%= moment === 'matin' ? 'Carte du Matin ☕' : 'Carte du Soir 🍸' %>
      </h1>

      <% Object.entries(carte).forEach(([category, items]) => { %>
        <% if (items && items.length) { %>
          <section class="mb-12">
            <h2 class="text-2xl font-semibold mb-4 capitalize border-b border-gray-700 pb-1 text-red-400"><%= category %></h2>
            <ul class="grid grid-cols-1 sm:grid-cols-2 gap-6">
              <% items.forEach(item => { %>
                <li class="flex items-start bg-gray-900 bg-opacity-80 p-4 rounded-lg shadow">
                  <% if (item.image) { %>
                    <img src="<%= item.image %>" alt="<%= item.name %>" class="w-20 h-20 object-cover rounded mr-4" />
                  <% } %>
                  <div>
                    <p class="text-lg font-medium text-white mb-1">
                      <%= item.name %>
                      <span class="text-sm text-gray-400"> – <%= item.price %></span>
                    </p>
                    <% if (item.description) { %>
                      <p class="text-sm text-gray-400"><%= item.description %></p>
                    <% } %>
                  </div>
                </li>
              <% }) %>
            </ul>
          </section>
        <% } %>
      <% }) %>

      <div class="text-center mt-12">
        <a href="/carte-selection" class="text-red-400 hover:underline">🔄 Revenir au choix matin/soir</a>
      </div>
    </div>
  </main>

  <%- include('../partials/footer') %>
</body>
</html>
